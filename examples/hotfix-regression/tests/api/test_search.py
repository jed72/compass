"""Illustrative tests for the hotfix-regression walkthrough.

The example exists to show the SHAPE of a Compass task, so these are stubs
rather than real tests of real code. They are real files with real function
names, though, because `declared-tests-resolve` checks that a scenario's
declared test exists on disk - a test that is merely NAMED is not a test, and
an example that fails the guardrail it demonstrates teaches the wrong thing.
"""


def test_empty_filter_object_does_not_crash():
    # Replace with a real assertion in your own project.
    assert True
