"""Illustrative tests for the feature-api-change walkthrough.

The example exists to show the SHAPE of a Compass issue, so these are stubs
rather than real tests of real code. They are real files with real function
names, because `declared-tests-resolve` checks that a declared test exists
on disk. An example that fails the guardrail it demonstrates teaches the
wrong thing.
"""


def test_429_includes_retry_after():
    # Replace with a real assertion in your own project.
    assert True

def test_over_limit_returns_429():
    # Replace with a real assertion in your own project.
    assert True

def test_per_client_isolation():
    # Replace with a real assertion in your own project.
    assert True

def test_under_limit_passes():
    # Replace with a real assertion in your own project.
    assert True

def test_window_resets():
    # Replace with a real assertion in your own project.
    assert True
