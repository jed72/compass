---
description: Retired name - this command is now /compass:define
---

# `/compass:specify` is now `/compass:define`

This command was renamed in v2. Run `/compass:define` - same stage, same
artifacts. The retired name is kept as this pointer for one major version.
