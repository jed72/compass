---
description: Retired name - this command is now /compass:design
---

# `/compass:plan` is now `/compass:design`

This command was renamed in v2. Run `/compass:design` - same stage, same
artifacts. The retired name is kept as this pointer for one major version.
