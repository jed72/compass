---
description: Retired name - this command is now /compass:triage
---

# `/compass:frame` is now `/compass:triage`

This command was renamed in v2. Run `/compass:triage` - same stage, same
artifacts. The retired name is kept as this pointer for one major version.
