---
description: Retired name - this command is now /compass:refine
---

# `/compass:clarify` is now `/compass:refine`

This command was renamed in v2. Run `/compass:refine` - same stage, same
artifacts. The retired name is kept as this pointer for one major version.
