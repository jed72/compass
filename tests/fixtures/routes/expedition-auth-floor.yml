# A small change that touches auth. RP-SHAPE-003 picks express as the
# candidate; RP-FLOOR-003 forces the route to at least expedition because
# `touches` includes a G5 domain.
name: expedition-auth-floor
assessment:
  risk: contained
  familiarity: brownfield-mapped
  size: small
  goal: delivery
  role: engineer
  labels: [auth]
expected:
  candidate_route: quick-fix
  delivery_approach: initiative
  fired_guardrail_ids: [RP-FLOOR-003]
  conflict: false
  stream_ceiling: null   # swarm is unbounded; only a cap produces a number
  has_gate: verify.security
