# A small change that touches auth. RP-SHAPE-003 picks quick-fix as the
# candidate; RP-FLOOR-003 raises the delivery approach to initiative because
# domain risk overrides size, and a one-line auth change is not small.
name: expedition-auth-floor
assessment:
  risk: contained
  familiarity: brownfield-mapped
  size: small
  goal: delivery
  role: engineer
  labels: [auth]
expected:
  candidate_route: quick-fix
  delivery_approach: initiative
  fired_guardrail_ids: [RP-FLOOR-003]
  conflict: false
  subtask_ceiling: null   # multiagent orchestration is unbounded; only a cap produces a number
  has_gate: verify.security
