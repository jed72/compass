# A small change that touches auth. RS-SHAPE-003 picks express as the
# candidate; RG-FLOOR-003 forces the route to at least expedition because
# `touches` includes a G5 domain.
name: expedition-auth-floor
assessment:
  risk: contained
  familiarity: brownfield-mapped
  size: small
  goal: delivery
  role: engineer
  labels: [auth]
expected:
  candidate_route: quick-fix
  delivery_approach: initiative
  fired_guardrail_ids: [RG-FLOOR-003]
  conflict: false
  topology: swarm
  has_gate: verify.security
