# Exploration intent that would touch auth - RP-FLOOR-003 would force
# delivery, but the candidate is Spike. That is the routing-conflict case
# and the evaluator MUST refuse (not silently promote).
name: spike-auth-conflict
assessment:
  risk: contained
  familiarity: brownfield-mapped
  size: small
  goal: exploration
  role: engineer
  labels: [auth]
expected:
  candidate_route: spike
  conflict: true
  conflict_message_contains: "routing conflict"
