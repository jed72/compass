# Fixture: v1 vocabulary used in its banned sense

<!-- Input for tests/test_terminology.py. Every ban in
     governance/terminology.yml must flag at least one line below; the
     scanner test fails if any ban goes unmatched here. This file lives
     under tests/, which the terminology scan exempts, so the planted
     vocabulary can never fail the repository scan itself. -->

Run /compass:frame to start; the Frame phase hands its judgement to the Needle.
The readings feed the computed route, recorded in route.md after route evaluate runs.
An Express task skips ceremony; an Expedition spans several streams.
The pipeline runs Frame → Specify → Clarify → Distribute → Land.
Read the spec through the product lens before the Land phase begins.
Score the blast_radius, the terrain, and the magnitude of the change.
Domain tags use touches_any and touches_common; note the blast radius too.
A hotfix owes a backfill before the task closes.
A checklist line tagged (backfill: BF-9) uses the renamed v1 tag, and the
spine key backfills: [] no longer exists either.
Settle it with compass backfill pay before shipping - the verb
renamed too.
Guardrail G1 and strategy S7 are cited by code in prose.
Intent lives in brief.md; the scenarios live in spec.feature.md.
The dimensions were scored during Frame, checked at verify and Land, and
the evaluator computes the route from them.
