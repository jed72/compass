# =============================================================================
# compass - the Compass CLI
# =============================================================================
# This rule only fires on cli/compass_pkg/, so the proof in
# test_pbw_e1_each_sweep_reports_a_planted_breach constructs the span
# directly at that path rather than reading this file - the banner line
# above is what that proof reuses.
