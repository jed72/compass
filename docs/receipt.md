# The receipt - `compass issue receipt`

The receipt is **one screen** that shows an issue's enforcement story end to end: the four-dimension assessment triage judged, the route the policy computed, the gates the issue cleared, and the typed evidence each gate cleared with. It is a *view* over the issue's recorded state - a render of what's on disk, not a re-run of the checks. Reading the receipt is reading the audit trail.

This is Compass's two differentiators made demonstrable in plain text - **per-issue computed routing** on the *Assessment* and *Route* lines, and **typed, gate-binding evidence** in the *Gates* and *Evidence* sections.

## Invoking

```
compass issue receipt --issue <slug>
```

Optionally `--workdir <path>` to point the receipt at a project rooted somewhere other than the current working directory. The command is **read-only**: it parses `task.yml`, `delivery-approach.md`, `evidence/`, and `governance/guardrails.yml`, prints the receipt to stdout, and mutates nothing on disk.

## Example

The receipt for a clean landed Standard issue. This output is **the actual receipt** for the fixture at `tests/fixtures/receipt-fixture-project/.compass/work/receipt-task/` - generated by running the command above. A test (`tests/test_receipt_render.py::test_docs_example_matches_actual_output`) re-runs it on every CI build and fails if the docs and the renderer disagree, so this example cannot drift.

```
================================================================================
Receipt - receipt-task (landed)
================================================================================

Assessment
----------
  risk            contained               the canonical fixture blast-radius justification
  familiarity     brownfield-mapped       the canonical fixture terrain justification
  size            standard                the canonical fixture magnitude justification
  goal            delivery (engineer)     the canonical fixture intent justification
  labels          public-api

Approach
--------
  feature  (topology: solo-or-pair)
  policy rules fired: none

Gates
-----
  verify.correctness         [ PASS ]           evidence: EV-001
  verify.governance          [ PASS ]           evidence: EV-002
  verify.traceability        [ PASS ]           evidence: EV-002
  verify.regression          [ PASS ]           evidence: EV-001
  verify.clarity             [ PASS ]           evidence: EV-003
  verify.security            [ PASS ]           evidence: EV-002

Evidence
--------
  EV-001    SCN-001 - the fixture scenario                                            test-run
  EV-002    check                                                                     command-output
  EV-003    reviewer: qa@example.com                                                  manual-review

================================================================================
Verdict: landed cleanly
```

## How the receipt is honest about weak state

The receipt's value is what it *won't* hide. The verdict markers and overall verdict line reflect the recorded state, faithfully:

| Recorded gate state | Receipt label | Effect on the overall verdict |
|---|---|---|
| `status: pass` cleared by evidence of an accepted type | `[ PASS ]` | landed cleanly |
| `status: pass` with **no evidence id** | `[ UNSUPPORTED ]` | landed with caveats |
| `status: pass` cleared by evidence whose type is not in `gate_evidence_requirements` for that gate | `[ TYPE-MISMATCH ]` | landed with caveats |
| `status: fail` | `[ FAIL ]` | FAILED - does not satisfy its own gates |
| `status: pending` | `[ PENDING ]` | not yet landed (header reads `IN PROGRESS`) |

Owed follow-ups are surfaced in their own *Follow-ups* section and counted in the verdict: "landed with caveats - N follow-up(s) owed".

## What the receipt does **not** do

- It does not re-run guardrail checks. For that, use `compass check`. The receipt reports what was actually recorded at ship time time, which is the audit-trail-from-disk property that makes the chain meaningful (ADR-005).
- It does not change exit codes based on the recorded state. The receipt exits **0** on a successful render, regardless of whether the issue landed cleanly, with caveats, or failed - those are *content*, not render failures. The non-zero exit is reserved for "render itself failed" (e.g. the issue does not exist). Use `compass check` if you need exit-code-driven enforcement in CI.
- It does not modify anything on disk - no caches, no temporary files, no edits to `task.yml`. `tests/test_receipt_render.py::test_receipt_is_read_only` is the regression net.

## Backward compatibility

The receipt renders `task.yml` files produced before this feature landed - including schema-1.0 (pre-`status` field) and issues with empty `evidence: []`. Sections backed by absent data are labelled `(not recorded)` rather than crashing; the header notes "schema 1.0 (legacy)" when the issue pre-dates schema 1.1. This is ADR-006 in action.

## Source

Implemented in `cli/compass` under the `# --- command: task receipt ---` block, adjacent to `cmd_task_lint`. Behavioural acceptance suite: `tests/test_receipt_render.py`. Fixture for this docs page: `tests/fixtures/receipt-fixture-project/`.
